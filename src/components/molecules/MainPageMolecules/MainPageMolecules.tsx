import { HeroMolecules } from '../Hero/HeroMolecules.tsx';
import { Garbage } from '../Garbage/Garbage.tsx';

export const MainPageMolecules = () => {
  return (
    <section>
      <HeroMolecules />
      <Garbage />
    </section>
  );
};
